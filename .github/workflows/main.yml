name: Build Debug APK

on:
  workflow_dispatch:  # Allows manual triggering from GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Cache Gradle
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.3'  # Stable version as of 2025; matches typical constraints for modern Flutter projects
        channel: 'stable'
        cache: true  # Caches Flutter pub dependencies automatically

    - name: Get Flutter dependencies
      run: flutter pub get

    - name: Build debug APK
      run: flutter build apk --debug --flavor debug
      # Uses default debug keystore for signing in CI environment—no custom keystore needed.
      # Flavor 'debug' as per README instructions ensures correct configuration.
      # Output: build/app/outputs/flutter-apk/app-debug.apk (installable on Android devices with unknown sources enabled).

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: vrcn-debug-apk
        path: build/app/outputs/flutter-apk/app-debug.apk
        retention-days: 30  # Artifacts available for 30 days in GitHub CI
